Config = {}

Config.Logs = {
    -- Enable logging for CFX resources (not synced, works locally only)
    -- This log is an example of logging during operations like restarting a script.
    _CFXprintEnabled             = true,

    -- Enable logging for resource management (synced)
    _ResourceManagerPrintEnabled = true,

    -- Enable logging for item addition operations (synced)
    _AddItemLogsEnabled          = true,

    -- Enable logging for code runner (synced)
    _CodeRunnerEnabled           = true,

    -- Enable logging for access tools (synced)
    _AccessToolsEnabled          = true,

    -- Enable logging for entity spawner (synced)
    _EntitySpawnerEnabled        = true 
}
